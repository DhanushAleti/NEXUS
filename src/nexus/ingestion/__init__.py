"""Ingestion subsystem (planned).

Reserved for the Ingestion layer described in ``docs/ARCHITECTURE.md`` §1,
which will normalize external inputs (Markdown, text, source code, Git history)
into provenance-tagged memory records. Not yet implemented; this package is an
intentional placeholder for deferred research work, not an incomplete build.
"""
